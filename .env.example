# =============================================================================
# MOVIE TICKETING API - ENVIRONMENT CONFIGURATION TEMPLATE
# =============================================================================
# Copy this file to app/.env and fill in the required values
# 
# Generate SECRET_KEY with:
# python -c "import secrets; print('SECRET_KEY=' + secrets.token_urlsafe(32))"
# =============================================================================

# REQUIRED - Generate a secure secret key
SECRET_KEY=REPLACE_WITH_GENERATED_SECRET_KEY

# REQUIRED - Admin credentials
ADMIN_EMAIL=admin@yourcompany.com
ADMIN_PASSWORD=your-secure-password

# OPTIONAL - Database (SQLite for dev, PostgreSQL for prod)
DATABASE_URL=sqlite:///./movie_ticketing.db

# OPTIONAL - Debug mode (true for dev, false for prod)
DEBUG=true
